"""simulation — RAWES core physics/flight-stack package.

Physics engine, ArduPilot mediator, control-law primitives, and shared
data models for the RAWES tethered autorotating rotor kite.

Sibling top-level packages (installed from the same repo root):
    arduloop   — ArduPilot GUIDED/rate control loop Python port
    envelope   — pumping envelope / point-mass sweep tool
    analysis   — SITL/simtest telemetry diagnostics
    viz3d      — 3D telemetry visualizers
    scripts    — calibration / hardware bench scripts
    tests      — all pytest suites (unit, simtests, sitl, hil, oneoff)
"""
